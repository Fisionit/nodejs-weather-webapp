<!DOCTYPE html>
<html>
  <head>
      <meta charset="utf-8">
      <title>NodeJS - Weather App</title>
      <link href="css/owfont-regular.min.css" rel="stylesheet" type="text/css">
      <link rel="stylesheet" type="text/css" href="/css/style.css">
      <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
      <link rel="icon" href="img/favicon.ico">
  </head>
  <body>
    <h1>Weather App - NodeJS</h1>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Get Weather">
        </form>
        </div>
        <% if(city !== null){ %>
          <ul>
              <li>
                  <h2><%= city %></h2>
                  <div>
                      <i class="owf owf-<%= weatherId %> owf-4x" style="opacity: 0.4" ></i>
                      <div class="temp"><%= tempF %><sup>°F</sup> / <%= tempC %><sup>°C</sup></div>
                  </div>
                  <div style="clear: both;"></div>
              </li>
          </ul>
        <% } %>
        <% if(error !== null){ %>
           <ul>
              <li>
                  <h2><%= error %></h2>
              </li>
            </ul>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
